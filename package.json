{
  "name": "pdc-nav",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production bun build.js",
    "watch": "NODE_ENV=production bun --watch build.js",
    "start": "bun run watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.25.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
