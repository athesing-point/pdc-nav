{
  "name": "pdc-nav",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun run build.js --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.25.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
